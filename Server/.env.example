# Proxmox Configuration
PROXMOX_IP=
PROXMOX_API_TOKEN=

# Proxy Server Configuration
PROXY_IP=0.0.0.0
PROXY_HTTP_PORT=80

# Ephemeral Port Range (for authenticated SPICE sessions)
PROXY_SPICE_PORT_MIN=40000
PROXY_SPICE_PORT_MAX=41000

# JWT Secret (auto-generated during install)
JWT_SECRET=

# TOTP Secrets (add your users here)
# Generate secrets with: python3 src/setup_totp.py <username>
# TOTP_SECRET_ALICE=
# TOTP_SECRET_BOB=
